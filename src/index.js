//index.js

import "./styles.css";
import { submitButton, closeButton, addTaskButton } from "./DOM";

document.addEventListener("DOMContentLoaded", () => {
    submitButton();
    closeButton();
    addTaskButton();
});
